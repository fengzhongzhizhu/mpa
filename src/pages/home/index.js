/* eslint-disable */
import '../../common/css/com.less';
import './index.less';
import {throttle} from '../../tools/utils';
console.log(throttle);
console.log(_.defaults({ 'a': 1 }, { 'a': 3, 'b': 2 }))//全局引入了lodash
console.log('首页的js运行了～～')